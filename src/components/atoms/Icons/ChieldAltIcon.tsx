import { SvgIcon, SvgIconProps } from '@mui/material';
import CHIELDALTSVG from '@/assets/Chield_alt.svg?react';

const ChieldAltIcon = (props: SvgIconProps) => {
  return (
    <SvgIcon {...props}>
      <CHIELDALTSVG />
    </SvgIcon>
  );
};

export default ChieldAltIcon;
